######
# ls #
######

# List
## Overwrite $ZSH/lib/directories.zsh
alias l='ls -h'
alias ll='ls -hl'
alias la='ls -hA'
alias lla='ls -hlA'

# List & Grep
alias lg='ls | grep'
alias lag='ls -A | grep'
alias llg='ls -hl | grep'
alias llag='ls -Ahl | grep'

