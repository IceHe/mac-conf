######
# ls #
######

# List
## Overwrite $ZSH/lib/directories.zsh
alias l='ls -h'
alias ll='ls -hl'
alias la='ls -hA'
alias lla='ls -hlA'

# List & Grep
alias lg='ls -h | grep'
alias llg='ls -hl | grep'
alias lag='ls -hA | grep'
alias llag='ls -hlA | grep'

